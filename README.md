This a linear Search program.
The logic is the target element is search one by one.
Suppose the array is {10,3,60,50,80}
Our target element is 50.
So we sesrch the target element's index.By uisng this algorithm.

LOGIC
int[] arr = {10,20,30,40,50}
int target = 30;
for(int i=0;i<arr.length;i++){
if(target == arr[i]){
System.out.println(i);
}
}
